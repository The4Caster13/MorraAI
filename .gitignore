node_modules/
dist/
.env
.env.local
*.log
.DS_Store
# Local scratch dir for downloaded audio. Anchored with a leading slash: a bare
# `storage/` matches any directory of that name at any depth, which silently
# excluded apps/server/src/storage/ — the module the server imports.
/storage/
apps/web/public/stimuli
